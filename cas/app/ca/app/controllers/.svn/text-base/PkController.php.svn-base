<?php

/**
 * PK接口控制器
 * 
 * @author win.sx
 *
 */
class PkController extends ControllerBase {
    protected $_type = array('CITY','SPAN_COUNTY','COUNTY','SCENIC','SPAN_CITY');
    protected function errResponse($code, $error){
        $this->_errorResponse($code,$error);die();
    }
    protected function succResponse($data){
        $this->_successResponse($data);die();
    }
}